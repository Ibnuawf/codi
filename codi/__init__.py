"""Codi — a zero-dependency Python code-health analyzer.

Scans a codebase with the `ast` module and produces per-function
complexity metrics, an import dependency graph, hotspot rankings,
and a self-contained HTML report.
"""

__version__ = "1.0.0"
